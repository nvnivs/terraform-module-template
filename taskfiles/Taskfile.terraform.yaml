---
version: '3'

tasks:
  env-init:
    desc: Initializes tfenv
    cmds:
      - tfenv install
      - tfenv use

  lint-init:
    desc: Initializes tflint
    cmds:
      - tflint -v
      - tflint --init
